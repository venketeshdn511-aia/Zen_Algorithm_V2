version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tradedeck_api
    ports:
      - "127.0.0.1:8001:8000"
    env_file: .env
    environment:
      - DEBUG=false
      - LOG_LEVEL=INFO
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 384M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s
    volumes:
      - ./tradedeck/logs:/app/logs:rw
    networks:
      - tradedeck_internal

  postgres:
    image: postgres:15-alpine
    container_name: tradedeck_db
    environment:
      POSTGRES_DB: tradedeck
      POSTGRES_USER: trader
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD required}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./tradedeck/deploy/postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trader -d tradedeck"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: 192M
    networks:
      - tradedeck_internal

  redis:
    image: redis:7-alpine
    container_name: tradedeck_redis
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD required}
      --maxmemory 64mb
      --maxmemory-policy allkeys-lru
      --save 60 100
      --appendonly yes
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 96M
    networks:
      - tradedeck_internal

  nginx:
    image: nginx:alpine
    container_name: tradedeck_nginx
    ports:
      - "8000:8000"
      - "443:443"
    volumes:
      - ./tradedeck/deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./tradedeck/deploy/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - api
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 64M
    networks:
      - tradedeck_internal
      - tradedeck_external

  prometheus:
    image: prom/prometheus:latest
    container_name: tradedeck_prometheus
    volumes:
      - ./tradedeck/deploy/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - promdata:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=30d
    ports:
      - "127.0.0.1:9090:9090"
    depends_on:
      - api
    profiles:
      - monitoring
    networks:
      - tradedeck_internal

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local
  promdata:
    driver: local

networks:
  tradedeck_internal:
    driver: bridge
    # Removed 'internal: true' to allow internet access for broker API and MongoDB Atlas
  tradedeck_external:
    driver: bridge
